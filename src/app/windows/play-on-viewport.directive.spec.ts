import { PlayOnViewportDirective } from './play-on-viewport.directive';

describe('PlayOnViewportDirective', () => {
  it('should create an instance', () => {
    const directive = new PlayOnViewportDirective();
    expect(directive).toBeTruthy();
  });
});
